# Copy this file and rename it to server.conf; then change IP address and ports according to your setup
127.0.0.1 			# IP Address
8080   				# Server Port
5006				# Bokeh Port
5010				# Bokeh Port (this is another bokeh server where we run problematic apps that might break the main server or where we do test runs)
5020				# Bokeh Port (this is another bokeh server for the math apps)
